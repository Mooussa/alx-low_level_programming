#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
*/
int main(void)
{
	int n;
	/*Looping through the numbers*/
	for (n = 48; n < 58; n++)
	{
		putchar(n);
	}
	putchar(10);
	return (0);
}
